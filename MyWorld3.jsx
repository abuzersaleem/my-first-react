import React from 'react'

const MyWorld3 = (props) => {
  return (
    
    <div>
        <h3> { props.Name} </h3>
        <p>Customer Email : { props.email}</p>
        <button onClick={() => this.NameChange()}>Change</button>
    </div>
    
  )
}

export default MyWorld3
